import React from "react";

// eslint-disable-next-line import/no-anonymous-default-export
export default props =>
<>
    <h1>Esses são os filhos</h1>
    <div>
        {props.children}
    </div>
  </>